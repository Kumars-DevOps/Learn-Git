- name: install apache web-server
  hosts: all
  become: yes
  tasks: 
  - name: install apache
    dnf:
      name: httpd
      state: present
  - name: start and enable apache web-service
    service::
      name: httpd
      state: started
      enabled: yes
